#include<stdio.h>

void DisplayDigits(iNo)
{
	iDigit = 0;
	while( iDigit = iNo%10)
	{
		iNo = iNo/10;
		
		printf("%d",iNo);
	}
	
}

int main()
{
	int iValue = 0;
	
	printf("Enter number\n");
	scanf("%d",&iValue);
	
	DisplayDigits(iValue);
	return 0;
}

